using System;
using System.Collections.Generic;
using System.Text;

namespace ProjectEuler.Net.Problems
{
    class Problem16
    {
        /* 
         static void Main(string[] args)
        {
            //using System.Numerics;
            findPowerDigitSum(1000);
        } 
        static void findPowerDigitSum(int power)
        {
            var biig = new BigInteger (Math.Pow(2, power));
            
            var sum = new BigInteger(0);
            while (biig != 0)
            {
                sum += biig % 10;
                biig = biig / 10;
            }

            Console.WriteLine(sum);
        }

        */
    }
}
