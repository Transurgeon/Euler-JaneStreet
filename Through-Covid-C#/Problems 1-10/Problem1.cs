using System;
using System.Collections.Generic;
using System.Text;

namespace ProjectEuler.Net
{
    class Problem1
    {
        /*static void Main(string[] args)
        {
            int[] multiples = { 3, 5 };
            Console.WriteLine(FindMultiplesSum(multiples, 1, 1000));
            static int FindMultiplesSum(int[] multiples, int low, int high)
            {

                int sum = 0;
                for (int i = low; i < high; i++)
                {
                    if (i % multiples[0] == 0)
                    {
                        sum += i;
                    }
                    else if (i % multiples[1] == 0)
                    {
                        sum += i;
                    }
                }
                return sum;
            }
        }
        */

    }
}
