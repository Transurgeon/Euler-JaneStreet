using System;
using System.Collections.Generic;
using System.Text;

namespace ProjectEuler.Net.Problems_1_10
{
    class Problem9
    { /*
        static void Main(string[] args)
        {
            specialPythagoreanTriplet(1000);
        }
        static void specialPythagoreanTriplet(int sum)
        {
            for (int a = 1; a < sum; a++)
            {
                for (int b = 1; b < sum - a; b++)
                {
                    int c = sum - a - b;
                    if (isPythagorean(a, b, c))
                        Console.WriteLine(a * b * c);
                }
            }

        }
        static Boolean isPythagorean(int a, int b, int c)
        {
            if (a * a + b * b == c * c)
                return true;
            if (c * c + b * b == a * a)
                return true;
            if (a * a + c * c == b * b)
                return true;

            return false;
        } */
    }
}
